class Kind:

    def __init__(self, behave, survive, replica, mutate, ratio, color, speed, span, name):
        self._behave = behave
        self._replica = replica
        self._mutate = mutate
        self._ratio = ratio
        self._color = color
        self._speed = speed
        self._span = span
        self._name = name
